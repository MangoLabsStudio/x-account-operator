# Mango X Ops 实习生验证教程

后台地址：<https://siriuszzz-api.uk/xops/>

当前后台没有访问密码。请勿把链接、X 密码、Cookie、Access Token 或验证码发给其他人。

当前 `XOPS_WRITES_ENABLED=false`，可以验证账号授权、创建计划和审批流程，但不会真的发帖或转帖。

设置账号资料前，先按 `PROFILE_PACK_ALTCOIN_MEME_10.md` 填写 Profile，再按 `PERSONA_PACK_ALTCOIN_MEME_10.md` 固定该账号的人设和写作语气。

## 一、授权 X 账号

1. 打开该账号对应的指纹浏览器环境。
2. 访问 X，确认已经登录正确账号。先核对右下角用户名，不要只看头像。
3. 在同一个指纹浏览器中打开后台地址。
4. 在“账号标签”填写便于识别的内部名称，例如 `AI-News-US-01`。
5. 点击“OAuth 连接 X”。
6. 在 X 授权页面再次核对用户名，然后同意授权。
7. 授权成功后会自动返回后台。
8. 查看“连接账号”区域，确认：
   - `label` 是刚填写的标签；
   - `username` 是本次要连接的 X 用户名；
   - 记录下该账号的 `id`，创建计划时要使用。

注意：授权链接有效期约 10 分钟。不要在同一个指纹浏览器里同时切换多个 X 账号。当前后台没有删除账号按钮，授权错账号后先停止，不要继续创建计划。

## 二、创建测试计划

为了避免未来开启真实写入后误发测试内容，验证时固定使用日期 `2099-12-31`。

将后台文本框内容替换为下面的 JSON。把 `account_id` 改成上一步显示的账号 `id`：

```json
{
  "account_id": 1,
  "plan_date": "2099-12-31",
  "actions": [
    {
      "at": "10:30",
      "type": "post",
      "text": "INTERNAL TEST — DO NOT PUBLISH"
    }
  ]
}
```

点击“保存为待审核”。页面下方出现新计划，且 `status` 为 `pending`，说明创建成功。

字段规则：

- `account_id`：连接账号区域显示的数字 ID。
- `plan_date`：`YYYY-MM-DD` 格式。
- `at`：北京时间，24 小时制 `HH:MM`。
- `type: post`：必须填写 `text`。
- `type: repost`：必须填写原帖的 `tweet_id`。
- 一个计划最多 20 个动作。

转帖测试格式如下，但验证阶段不建议使用真实帖子：

```json
{
  "at": "15:00",
  "type": "repost",
  "tweet_id": "1234567890"
}
```

`tweet_id` 是帖子链接最后一段数字。例如：

```text
https://x.com/example/status/1234567890
                             └─ tweet_id
```

## 三、审批并检查队列

1. 找到刚创建的计划。
2. 再次核对 `account_id`、`username`、日期和文案。
3. 点击“审批计划 + 编号”。
4. 刷新页面。
5. 确认计划状态变为 `approved`。
6. 在“执行队列”确认出现对应任务，状态为 `scheduled`。

当前写入关闭，所以任务会一直保持 `scheduled`，X 上不会出现帖子。这是正常结果。

## 四、验证通过标准

以下项目全部满足即为通过：

- 后台无需密码即可打开。
- OAuth 可以跳转到 X 官方授权页面。
- 授权后显示正确的 `username` 和账号 ID。
- 测试计划可以保存为 `pending`。
- 审批后计划变为 `approved`。
- 执行队列出现 `scheduled` 任务。
- X 账号没有产生真实帖子或转帖。

## 五、提交验证结果

提交以下信息：

- 指纹浏览器环境名称；
- X 用户名；
- 后台账号 ID；
- 授权时间；
- 账号列表、计划状态、执行队列三张截图；
- 是否出现错误，以及完整错误文字。

截图中不要包含 X 密码、验证码、Cookie、Client Secret、Access Token 或浏览器开发者工具内容。

## 常见问题

### 授权后显示了错误账号

立即停止。不要创建计划。回到对应指纹浏览器确认当前登录账号，再重新授权。

### 点击保存后报错

检查 JSON 是否使用英文双引号、末尾是否多了逗号，以及 `account_id`、日期、时间格式是否正确。

### 页面没有更新

直接刷新浏览器页面。后台目前没有单独的刷新按钮。

### 为什么 X 上没有发帖

因为真实写入开关当前处于关闭状态。这次只验证授权、计划和审批链路。
